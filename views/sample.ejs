<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Items</title>
    <link rel="stylesheet" href="public/css/style.css">
    <script src="public/js/login.js"></script>
    <script src="public/js/list.js"></script>
    <script src="public/js/validate.js"></script>
</head>
<body>

    <nav>
        <form action="/logout_ofItems" method="POST">
            <button type="submit">Logout</button>
        </form>
        <a href="/go" style="margin-left: 85%;">Home</a>
    </nav>

    <div class="center-button">
        <p>
            Press this button to add all of the contents below to your page.<br>
            If you would not like to add an item you can remove it before<br>
            hitting this button. If you would not like to add any of these<br>
            items you can press home in the upper right corner.
        </p>
        <button onclick="location.href='/add_items_home'">Confirm</button>
    </div>

    <div class="container">
        <h1>Items</h1>
        <ul>
            <% test.forEach((item, index) => { %>
                <li>
                    <strong>Name:</strong> <%= item[0] %><br>
                    <strong>Quantity:</strong> <%= item[1] %><br>
                    <strong>Price:</strong> <%= item[2] %><br>
                    <img src="<%= item[3] %>" alt="<%= item[0] %>">
                    <button class="delete-btn" onclick="removeItem(<%= index %>)">Delete</button>
                </li>
            <% }); %>
        </ul>
    </div>

</body>
</html>
